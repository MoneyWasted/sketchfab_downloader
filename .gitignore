node_modules/
__pycache__/
.DS_Store

# Working cache (per-model downloads, decrypted files, descrambled textures)
.cache/

# Cached viewer JS bundles and deobfuscated output (RE artifacts)
viewer_js/
deobfuscated/

# NPM lock
package-lock.json

# Extracted WASM (auto-downloaded at runtime)
decrypt.wasm
.wasm_backups/

# Output GLB files
*.glb
